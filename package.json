{
  "name": "intabide-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:ai": "npm run dev --workspace=ai-service",
    "dev:auth": "npm run dev --workspace=auth-service",
    "dev:frontend": "npm run dev --workspace=frontend",
    "build:auth": "npm run build --workspace=auth-service"
  }
}
